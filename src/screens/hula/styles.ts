import {StyleSheet} from 'react-native';
import {HulaStyles} from './types';

const styles = StyleSheet.create<HulaStyles>({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default styles;
