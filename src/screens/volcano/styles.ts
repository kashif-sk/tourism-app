import {StyleSheet} from 'react-native';
import {VolcanoStyles} from './types';

const styles = StyleSheet.create<VolcanoStyles>({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default styles;
